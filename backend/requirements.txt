# --- runtime ---
fastapi==0.121.3          # bumped from 0.111.0 to admit starlette 0.49.1
starlette==0.49.1         # fixes GHSA-f96h-pmfr-66vw, GHSA-2c2j-9gv5-cj73, GHSA-7f5h-v6xp-fcq8
uvicorn[standard]==0.30.0
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
pydantic==2.7.0
pydantic-settings==2.3.0
paramiko==5.0.0          # bumped from 3.4.0 — fixes CVE-2026-44405
httpx==0.27.0
python-dotenv==1.2.2      # bumped from 1.0.1 — fixes GHSA-mf9w-mj56-hr94
apscheduler==3.10.4
xhtml2pdf==0.2.16         # replaces weasyprint — pure Python, drops the
                          # cairo/pango/gdk-pixbuf system lib stack from
                          # both image variants and unblocks the
                          # Hummingbird hardened backend image. See PR #12.
svglib<1.6.0              # xhtml2pdf hard-requires svglib; 1.6.0+ added
                          # rlpycairo→pycairo→system-cairo which defeats
                          # the "pure Python" win. Pinning <1.6 keeps
                          # the dep tree cairo-free. Our reports don't
                          # use SVG, so the older renderer suffices.
pyyaml==6.0.1
openpyxl==3.1.5           # server-side RVTools parsing (read-only streaming mode)
python-multipart==0.0.20  # FastAPI UploadFile / Form for POST /api/imports/rvtools
filelock==3.20.3          # explicit pin — fixes GHSA-w853-jp5j-5j7f, GHSA-qmgc-5h2g-mvrw

# --- dev / CI only (kept here per project convention) ---
ruff==0.4.4
mypy==1.10.0
pytest==9.0.3             # bumped from 8.2.0 — fixes GHSA-6w46-j5rx-g56g
pytest-asyncio==1.3.0     # bumped from 0.23.6 — pytest 9 compatibility (1.0.0 caps pytest<9)
pytest-cov==6.0.0         # bumped from 5.0.0 — pytest 9 compatibility

# --- security scanners (CI-only; run via .github/workflows/ci.yml) ---
bandit==1.7.9
pip-audit==2.7.3
safety==3.7.0             # bumped from 3.2.7 — older safety pins filelock~=3.12.2
