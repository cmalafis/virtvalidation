apiVersion: v1
kind: Service
metadata:
  name: {{ include "virtvalidate.fullname" . }}-backend
  labels:
    {{- include "virtvalidate.componentLabels" (dict "root" . "component" "backend") | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "virtvalidate.componentSelectorLabels" (dict "root" . "component" "backend") | nindent 4 }}
